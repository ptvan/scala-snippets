object Playground extends App {
    // assignments
    val secretNumber: Int = 31337

    // string operations
    val composedString = "strings" + " " + "can" + " " + "be" + " " + "concatenated"
    val interpolatedString = "the secret is" + s"$secretNumber !"
    
}